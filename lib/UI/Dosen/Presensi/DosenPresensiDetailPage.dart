import 'package:flutter/material.dart';

class DosenPresensiDetailPage extends StatelessWidget {
  const DosenPresensiDetailPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
