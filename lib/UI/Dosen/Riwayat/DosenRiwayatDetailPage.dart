import 'package:flutter/material.dart';

class DosenRiwayatDetailPage extends StatelessWidget {
  const DosenRiwayatDetailPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
