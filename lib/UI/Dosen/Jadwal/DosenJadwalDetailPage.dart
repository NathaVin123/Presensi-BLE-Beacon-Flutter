import 'package:flutter/material.dart';

class DosenJadwalDetailPage extends StatelessWidget {
  const DosenJadwalDetailPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
