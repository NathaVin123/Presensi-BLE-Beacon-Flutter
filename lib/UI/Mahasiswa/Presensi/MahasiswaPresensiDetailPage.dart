import 'package:flutter/material.dart';

class MahasiswaPresensiDetailPage extends StatelessWidget {
  const MahasiswaPresensiDetailPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
