import 'package:flutter/material.dart';

class MahasiswaJadwalDetailPage extends StatelessWidget {
  const MahasiswaJadwalDetailPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
